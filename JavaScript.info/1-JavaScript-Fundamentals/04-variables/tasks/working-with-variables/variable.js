// Working with variables:

// Declare two varaibles: admin and name.
let admin;
let name;

// Assign the value "John" to name.
name = "John";

// Copy the value from name to admin.
admin = name;

// Show the value of admin using 'alert'.
alert(admin);
